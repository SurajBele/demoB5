apiVersion: v1
kind: service
metadata:
  name: my-svc
  labels:
    name: my-svc
    env: Devops
spec:
  type: ClusterIP
  selector:
    name: my-pod
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80

    